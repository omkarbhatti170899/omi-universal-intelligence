# Omi Orchestration v8

A single orchestration boundary now coordinates:
request routing → optional controlled research → provider generation → verification → audit.

Research remains opt-in and constrained by the existing HTTPS allowlist, timeout and size
limits. Provider credentials remain environment-only. External source content is treated
as untrusted evidence rather than instructions.
